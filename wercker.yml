box: rocker/hadleyverse
build:
  steps:
    - script:
        name: install and build blogdown
        code: |
          Rscript build.R
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        branch: master
        token: $GIT_TOKEN
        basedir: public
