box: debian
build:
  steps:
    - script:
        name: echo
        code: |
          echo "hello world!"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        basedir: public
